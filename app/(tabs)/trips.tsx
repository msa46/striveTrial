import 'react';
import { Text, View } from 'react-native';

const trips = () => {

    return (
        <View style={{flex:1, justifyContent:'center', alignItems:'center', backgroundColor:"black"}}>
            <Text className="color-white">TRIPS</Text>
        </View>
    )
}

export default trips;
