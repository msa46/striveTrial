import { Text, View } from 'react-native';

const  planner = () => (
    <View style={{flex:1, justifyContent:'center', alignItems:'center', backgroundColor:"black"}}>
        <Text className='color-white'>Another route</Text>
    </View>
)

export default planner;